class CategoryModel {
  int? id;
  bool? isSelected;
  String? title;
  String? icon;

  CategoryModel({
    required this.id,
    required this.isSelected,
    required this.title,
    required this.icon,
  });
}
